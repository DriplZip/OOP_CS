namespace Presentation.Constants;

internal static class PolicyName
{
    internal const string EmployeePolicy = nameof(EmployeePolicy);
    internal const string DirectorPolicy = nameof(DirectorPolicy);
}